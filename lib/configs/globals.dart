import 'package:flutter/material.dart';

// Network
const baseURL = "https://99wzcs8tx0.execute-api.us-east-1.amazonaws.com/dev";

// Canvas Colors
const canvasColor = Color.fromRGBO(247, 247, 247, 1);

// Theme Colors
const primaryColor = Color.fromRGBO(26, 68, 237, 1);
const secondaryColor = Color.fromRGBO(87, 85, 230, 1);

// Button Shadows
const primaryShadow = Color.fromRGBO(26, 68, 237, .3);

// Primary Shadow Properties
Color shadowColor = Colors.grey[350];
const shadowOffset = Offset(0, 14);
double shadowBlur = 35.00;
